# Sandbox Experiments

Place preview-only design experiments in this folder.

- Sandbox assets can be referenced only when running `cf preview ... --mode=sandbox`.
- Sandbox assets must never be imported into production SCORM builds.
